-@members.each do |member|
	= image_tag member.image.url(:thumb)
	= member.name
	= link_to member.company_name, "http://#{member.company_webpage}", :target => "_blank"
	- if user_signed_in?
		= link_to "Edit Member", edit_member_path(member)

%br
= render 'map'