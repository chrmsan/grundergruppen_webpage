= simple_form_for @member do |f|
	= f.input :name
	= f.input :image
	= f.input :company_name
	= f.input :company_webpage
	= f.button :submit, data: { confirm: "Is Member information correct?" }

=link_to "Cancel", root_path